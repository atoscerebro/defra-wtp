import { Link, Paragraph } from 'govuk-react';
import styled from 'styled-components';

export const LinkWrapper = styled(Paragraph)`

`;

export const FooterWrapper = styled.div`
    position: absolute;
    bottom: 0;
`;